{
  "description": "Storefront safety hooks for hairsolutions.co. SessionStart prints a slim repo/branch status; PreToolUse blocks Shopify CLI + dev-server deploys; PostToolUse gives a non-blocking design-system nudge on theme edits. Claude Code/Cowork schema; each script reads the JSON payload on stdin.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/session_preflight.py\"", "timeout": 6 }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/pre_command_guard.py\"", "timeout": 5 }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          { "type": "command", "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/post_edit_validate.py\"", "timeout": 10 }
        ]
      }
    ]
  }
}
