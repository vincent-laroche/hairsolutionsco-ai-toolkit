# Design-system reference material

These files are retained because they are distinct from the authoritative master system. They are historical or specialized references, not the current implementation source.

Exact copies of files already present in `../master/` were removed during curation.
